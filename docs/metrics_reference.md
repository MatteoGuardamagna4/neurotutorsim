# Metrics reference (§6.5, decision gate 19)

Generated by `scripts/build_metrics_reference.py` from the docstrings in
`src/analysis/neural_metrics.py`. **Do not edit by hand** -- rerun the script.

Gate 19: no cortical metric is interpreted until its direction and meaning are
documented. `Direction: meaningful` means a larger value has one interpretation;
`ambiguous` means it does not, and the metric may only be read as a contrast
between matched conditions.

Every quantity below is a property of a *predicted* cortical response from an
encoding model. None of them is a measurement of a brain, and none is a measure
of learning.

## Per-parcel time-series metrics

### `mean_response`

`mean_response(series: 'Sequence[float]') -> 'float'`

Average predicted response over the instructional window.

- **Formula:** (1/T) * sum_t x_t, over the samples inside the instructional window.
- **Units:** predicted BOLD units (arbitrary, TRIBE's output scale).
- **An increase means:** a higher average predicted response across the window.
- **Direction:** ambiguous -- a larger predicted response is not by itself better or worse learning; it is only interpretable relative to a matched contrast condition.

### `peak_response`

`peak_response(series: 'Sequence[float]', kernel: 'str', width_s: 'float', tr_seconds: 'float') -> 'float'`

Maximum of the temporally smoothed response.

- **Formula:** max_t (x * k)_t, where k is the configured smoothing kernel.
- **Units:** predicted BOLD units.
- **An increase means:** a stronger maximal response once high-frequency fluctuation has been smoothed away.
- **Direction:** ambiguous -- peak amplitude conflates the size of a response with how concentrated it is in time.

### `auc`

`auc(series: 'Sequence[float]', tr_seconds: 'float') -> 'float'`

Area under the response curve, equation (8).

- **Formula:** trapezoidal integral sum_t (x_t + x_{t+1}) / 2 * TR.
- **Units:** predicted BOLD units x seconds.
- **An increase means:** more total predicted response integrated over the window -- a larger response, a longer one, or both.
- **Direction:** meaningful -- AUC is the quantity §8.2 standardizes into Z(u,c,p), and larger unambiguously means more integrated predicted response. It does not, on its own, mean more learning.

### `time_to_peak`

`time_to_peak(series: 'Sequence[float]', kernel: 'str', width_s: 'float', tr_seconds: 'float') -> 'float'`

Latency of the smoothed maximum, from the start of the window.

- **Formula:** TR * argmax_t (x * k)_t.
- **Units:** seconds.
- **An increase means:** the strongest predicted response arrives later in the stimulus.
- **Direction:** meaningful -- later is later. Whether late peaks are desirable is a separate question this metric does not answer.

### `sustained_engagement`

`sustained_engagement(series: 'Sequence[float]', tr_seconds: 'float', baseline_percentile: 'float', threshold_sd: 'float') -> 'float'`

Time spent above a stimulus-specific baseline threshold.

- **Formula:** TR * #{t : x_t > b + k*sd(x)}, where b is the `baseline_percentile` percentile of this stimulus's own series and k is `threshold_sd`.
- **Units:** seconds.
- **An increase means:** the predicted response stays elevated above its own baseline for longer.
- **Direction:** meaningful -- a longer time above threshold is unambiguously more sustained. The baseline is per stimulus, so this is not comparable in absolute terms across stimuli of different overall amplitude.

## Whole-cortex spatial metrics

### `spatial_dispersion`

`spatial_dispersion(parcel_values: 'Sequence[float]') -> 'float'`

Spread of the response magnitude across parcels.

- **Formula:** population standard deviation over parcels, sd_p(x_p) with ddof=0.
- **Units:** same units as the parcel summary it is computed on (BOLD x seconds when the summary is |AUC|).
- **An increase means:** response magnitude is distributed more unevenly across cortex -- some parcels far above others.
- **Direction:** ambiguous -- high dispersion is consistent with a focal, well-targeted response and with a noisy one, and the metric cannot distinguish them.

### `spatial_entropy`

`spatial_entropy(parcel_values: 'Sequence[float]') -> 'float'`

Shannon entropy of the normalized spatial response profile, equation (9).

- **Formula:** p_p = x_p / sum_p x_p ; H = -sum_p p_p * log(p_p), natural log, with the convention 0*log 0 = 0.
- **Units:** nats (0 = all response in one parcel; log(P) = perfectly uniform).
- **An increase means:** predicted response is spread more evenly across parcels.
- **Direction:** meaningful -- higher entropy is unambiguously a flatter spatial distribution. Whether flatter is better is not a claim this metric makes.

### `network_integration`

`network_integration(parcel_timeseries: 'np.ndarray', networks: 'Sequence[str]') -> 'float'`

Ratio of between-network to within-network parcel coupling.

- **Formula:** mean r(p, q) for parcel pairs in different networks, divided by mean r(p, q) for pairs in the same network, where r is the Pearson correlation of the two parcels' predicted time series.
- **Units:** dimensionless ratio (1.0 = between-network coupling as strong as within-network; < 1 = networks are segregated).
- **An increase means:** parcels in different networks behave more alike -- a more integrated, less segregated predicted response.
- **Direction:** meaningful -- higher is more integrated. Note this is the inverse sense of the segregation indices used elsewhere in the fMRI literature, so the sign convention must be stated whenever it is reported.

## Cross-stimulus metrics

### `representational_differentiation`

`representational_differentiation(patterns_by_concept: 'Mapping[str, np.ndarray]') -> 'float'`

How distinct different concepts' cortical patterns are from each other.

- **Formula:** mean over concept pairs of 1 - r(z_a, z_b), where z is a concept's parcel-pattern vector and r is the Pearson correlation across parcels.
- **Units:** correlation distance (0 = identical patterns, 1 = uncorrelated, 2 = perfectly anticorrelated).
- **An increase means:** concepts are represented more distinctly from one another.
- **Direction:** meaningful -- higher is more differentiated.

### `within_concept_consistency`

`within_concept_consistency(patterns_by_variant: 'Mapping[str, np.ndarray]') -> 'float'`

How reproducible one concept's cortical pattern is across its variants.

- **Formula:** mean over variant pairs of r(z_i, z_j), Pearson correlation across parcels between two variants of the same concept.
- **Units:** correlation coefficient in [-1, 1].
- **An increase means:** regenerations and variants of the same concept produce more similar predicted patterns -- a more stable representation.
- **Direction:** meaningful -- higher is more consistent.

## Summary

- metrics with a directionally meaningful increase: **7**
- metrics whose direction is ambiguous: **3**

Whole-cortex metrics are stored with `parcel_id = -1` and computed over the `abs_auc` spatial summary.
